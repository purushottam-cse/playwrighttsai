** Prompt **

Explain (concept) in a table view with clear comparison columns. use a simple code/file exmple to walk through each layer. save it as a '.md' file inside the 'InterviewQuestionNOtes' folder. Follow the same style as 'X_Y_Z_IQ.md' - breakdown the table, example walkthrough, pipeline diagram, and TL; DR.